# MQTT broker (public HiveMQ — no auth needed)
MQTT_BROKER=mqtt://broker.hivemq.com
MQTT_TOPIC=silabs-prep/envmonitor/readings

# HTTP / Socket.io server
PORT=4000

# Max readings kept in the in-memory ring buffer
HISTORY_SIZE=100
